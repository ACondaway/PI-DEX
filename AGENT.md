# PI-DEX 开发规范

## 1. 文档目的与规则强度

本文档约束 PI-DEX 仓库中的设计、实现、测试和交付。目标是让代码变更可复现、可验证，并避免项目代码与上游 OpenPI、原始数据格式之间产生隐式耦合。

- **必须 / 不得**：强制要求。
- **应 / 不应**：默认要求；只有存在明确理由时才能偏离，并需在交付说明中记录原因和风险。
- **可以**：可选建议。

本文档适用于整个仓库。若子目录后续增加更具体的规范，则以离目标文件最近的规范为准；用户在当前任务中的明确要求优先级最高。

## 2. 项目定位

PI-DEX 基于 OpenPI 的 π0.5（源码标识为 `pi05`）和 Sharpa North 机器人本体开展研究，理论参考 [Ego-PI](https://arxiv.org/abs/2606.08107)。

项目的长期目标是构建带触觉反馈的快慢系统：

- 慢系统：VLA 模型生成低频动作块和高层行为意图。
- 快系统：轻量模块根据最新观测对动作进行高频修正。
- 触觉编码器：由外部代码库维护；本仓库只保留稳定、可替换的触觉特征接口。

除非已有代码和测试能够证明某项能力已经完成，否则上述内容应视为**设计目标**，不得在文档或交付说明中描述为已实现功能。

## 3. 仓库结构与修改边界

- `src/pi_dex/`：PI-DEX 自有 Python 包的预定位置。新增的数据适配、动作编解码、快慢系统接口和训练扩展默认放在这里。
- `data/schema.json`：SharpaOpenData 单个 episode 的当前描述性格式基准；它不是可直接用于机械校验的完整 JSON Schema。
- `openpi/`：上游 OpenPI 代码快照及其 Python 工具链。

仓库根目录通过 `pyproject.toml` 提供独立、可安装的 `pi-dex` 包和测试入口。根包不得依赖临时 `PYTHONPATH` 或当前工作目录才能导入；修改依赖声明后仍必须按第 6.4 节更新并提交根 `uv.lock`。

复用 OpenPI 时，按以下优先级选择实现方式：

1. 直接导入并复用稳定接口；
2. 通过组合、继承、适配器或配置注入扩展；
3. 只有无法在 `src/pi_dex/` 中隔离实现时，才对 `openpi/` 做最小修改。

不得复制大段 OpenPI 源码到 `src/pi_dex/`。确需修改 `openpi/` 时，必须同时：

- 说明无法通过外部适配实现的原因；
- 保持补丁小且聚焦；
- 为上游原行为和 PI-DEX 扩展补充兼容性测试；
- 在交付说明中列出上游差异，便于未来同步 OpenPI。

不得顺带进行无关重构、全仓格式化或依赖升级。`third_party/`、`transformers_replace/`、生成文件和锁文件应视为特殊区域；没有明确需求时不要修改或格式化它们。

## 4. 模型与动作空间约定

### 4.1 单手逻辑动作

目标单手动作由腕部位置、腕部 6D 连续旋转表示和手指关节角拼接而成：

$$
\tilde{a}_{\tau}^{s}
= [p_{\tau}^{s};\ r_{\tau,6D}^{s};\ q_{\tau,\mathrm{hand}}^{s}]
\in \mathbb{R}^{3+6+22}
= \mathbb{R}^{31},
\qquad s \in \{L,R\}.
$$

其中：

- $p \in \mathbb{R}^{3}$ 是腕部位置，不是完整位姿；
- $r_{6D} \in \mathbb{R}^{6}$ 是连续旋转表示，不代表 6 个旋转自由度；
- $q_{\mathrm{hand}} \in \mathbb{R}^{22}$ 是单手 22 个关节的关节角。

为兼容 π0.5 的 32 维动作投影，逻辑动作在末维补零：

$$
a_{\tau}^{s} = \operatorname{pad}_{32}(\tilde{a}_{\tau}^{s})
= [\tilde{a}_{\tau}^{s}; 0] \in \mathbb{R}^{32}.
$$

第 32 维没有机器人语义，推理解码时必须丢弃。补零和解包必须集中在唯一的动作转换模块中；归一化和损失实现必须复用该模块声明的有效维度或 mask，不得各自硬编码索引。当前固定流程先对每手 31 个有效维度归一化，再补第 32 维零值，因此 normalization asset 只保存 `[31]` 统计量且无需 padding 统计量；若未来改为对已 padding 的 32 维张量归一化，才必须为 padding 维补中性统计量。padding 维不得参与损失；若未来改变此约束，必须明确记录并用测试固定该行为。

### 4.2 数据原始动作与派生动作

`data/schema.json` 中的原始控制指令（commanded action）并不是上述 31 维末端动作。每侧字段主要是：

- `action/<side>_arm/joint_angle`：7 维手臂关节命令；
- `action/<side>_hand/joint_angle`：22 维手部关节命令。

31 维动作是项目的**目标派生表示**。从关节命令生成腕部位置和 6D 旋转时，必须明确并验证：

- 正向运动学模型和机器人标定版本；
- 世界坐标系、基座坐标系或末端坐标系；
- 旋转表示的定义、分量顺序和转换实现；
- 动作是绝对量、相对量还是 residual；
- 单位、关节顺序及左右手镜像规则。

不得把可选的 `state/<side>_arm/tcp_pose` 直接当成控制指令。该字段只有 6 维、旋转约定尚需消歧，而且部分数据可能全零；使用前必须检查存在性和有效性。

### 4.3 双手交错与动作时域

令 $K$ 表示双手同时推进的物理控制步数。左右手动作沿模型序列轴按 `L, R` 交错：

$$
A_t = [a_t^L, a_t^R, a_{t+1}^L, a_{t+1}^R, \ldots,
a_{t+K-1}^L, a_{t+K-1}^R]
\in \mathbb{R}^{2K \times 32}.
$$

对采用上述双手交错编码的 PI-DEX 配置，模型 action horizon 定义为 $H_{model}=2K$，因此必须为偶数。该约束不适用于 OpenPI 原有的非交错配置。每个物理控制时刻的有效双手动作是 62 维，在模型中占用两个 32 维连续动作槽位。除非特指网络投影后的内部表示，不要把这些连续动作向量称为 “action token”。

当前 OpenPI 数据加载器默认把 horizon 解释为连续时间步，并不具备左右手交错语义。实现 PI-DEX 时必须提供成对的：

- 训练侧 `interleave(left, right)`；
- 推理侧 `deinterleave(actions)`。

二者必须通过往返测试，并使用可区分的左右手样本覆盖奇数 horizon、手别顺序和末帧边界。PI-DEX 交错路径遇到奇数 $H_{model}$ 时必须立即报错，不得截断或补齐后继续运行。

### 4.4 快慢系统接口

慢系统输出、快系统输入和最终控制命令之间的接口必须明确声明：

- 张量 shape、dtype、device 和 batch 维；
- 输入与输出频率、时间戳及允许延迟；
- 坐标系、单位和动作语义；
- 快系统输出的是绝对动作、增量还是 residual；
- 触觉特征缺失、延迟或无效时的降级策略；
- 限幅、安全检查及回退行为。

快系统不得静默改变慢系统的动作约定。外部触觉编码器应通过协议或适配器接入，不得把其具体实现和私有依赖复制进本仓库。

## 5. 数据处理规范

### 5.1 时间轴与对齐

数据包含两个采样率：

- 图像、触觉和 mode 使用约 30 Hz 的对齐时间轴，长度为 $N$；
- state 和 action 使用约 60 Hz 的原始时间轴，长度为 $M$，通常 $M \approx 2N$。

从对齐帧 $k$ 读取 state/action 时，必须使用对应 group 的 `aligned_index[k]`。不得用 `2 * k` 代替真实索引，也不得假设所有 episode 都严格满足 $M=2N$。

动作 horizon 使用 30 Hz 对齐步还是 60 Hz 原始控制步，必须由配置显式指定，并体现在变量名、注释和测试中。一个 batch 内不得混用时间基准。

### 5.2 Schema 与可选字段

- 数据读取器和转换代码必须以 `data/schema.json` 记录的保证为实现基线，不得依赖其中未保证存在的字段。
- 若真实数据与 schema 不一致，必须保留可复现的样本证据，并在同一改动中更新 schema、兼容策略和测试；不得为了迁就单个样本而静默改写通用语义。
- 可选字段必须显式处理“存在且有效”“缺失”“存在但无效”三种情况。
- 对兼容字段名（例如 schema 记录的单复数别名）应在唯一适配层统一，不得让别名扩散到模型代码。
- 读取 padded JPEG 时必须按 JPEG EOI 标记裁剪，不能简单删除末尾的 `0x00`。
- annotation 的区间端点是闭区间；转换为 Python slice 时必须处理右端点，避免少一帧。
- 数据异常不得被静默修复。若必须容错，应记录 episode、字段、原值、处理方式和统计数量。

### 5.3 数组与张量边界

跨模块传递数组或张量时，必须明确并在入口校验：

- shape 和各轴语义；
- dtype 与数值范围；
- NumPy、JAX 或 PyTorch 后端及 device；
- 单位、坐标系、手别和关节顺序；
- 时间戳、采样率和 mask 语义。

错误信息应至少包含字段名、期望值和实际值。训练或推理热路径中应避免无意的 host/device 拷贝、逐帧 Python 循环和隐式同步。

## 6. Python 代码规范

### 6.1 版本与格式

- `openpi/` 主包以 Python 3.11 为基线，遵循其 `pyproject.toml` 中的 Ruff 配置和 120 字符行宽。
- `openpi/packages/openpi-client/pyproject.toml` 声明 Python 3.7 为兼容目标。现有源码仍有少量不兼容语法，这属于待清理的技术债，不能作为继续引入新不兼容写法的先例；修改相关文件时应修复触及的兼容问题，或在交付说明中明确记录阻碍。
- 导入必须由 Ruff 排序；禁止通配符导入。优先使用绝对导入，并沿用相邻模块的别名风格。
- 新增或修改的公共函数、类、配置项必须提供类型标注；涉及数组时优先沿用 `array_typing`、`jaxtyping` 和附近代码的运行时检查方式。
- 公共接口必须有 docstring，说明输入、输出、行为及可能抛出的异常；涉及数组或物理量时，还必须说明 shape 和单位。注释应解释“为什么”，不要逐行复述代码。

### 6.2 设计与命名

- 模块、函数、变量使用 `snake_case`，类使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。
- 名称应包含业务语义；避免 `data1`、`tmp2`、`process`、`handle` 等无法表达作用的命名。
- 单个函数应只承担一个清晰职责。数据解析、纯转换、模型调用和 I/O 应尽量分离。
- 配置对象应优先保持不可变，并遵循附近代码使用的 frozen dataclass / protocol 模式。
- 布尔参数应使用关键字传递；多个布尔开关应考虑改为枚举或独立配置对象。
- JAX 随机数必须显式传递和拆分 PRNG key；不得依赖隐藏的全局随机状态。
- 同时支持 JAX 和 PyTorch 的公共逻辑必须保证语义一致，并分别覆盖至少一个针对性测试。

### 6.3 异常、日志与配置

- 在最靠近数据边界的位置尽早失败；禁止空的 `except`、笼统捕获后忽略异常或用默认值掩盖损坏数据。
- 只捕获能够恢复且恢复策略明确的异常；重新抛出时保留原异常链。
- 库代码优先使用模块级 logger；面向命令行用户的脚本可以使用 `print` 输出结果。
- 不得硬编码本机绝对路径、GPU 编号、数据集目录、checkpoint、凭据、服务地址或实验名称；使用配置、CLI 参数或环境变量。
- 不得在日志中输出密钥、访问令牌或大批原始样本。

### 6.4 依赖与生成文件

- 使用 `uv` 管理 OpenPI 环境和依赖，不得直接调用 `pip` 改写环境状态。
- 修改依赖声明后使用 `uv lock` 更新 `uv.lock`，不得手工编辑锁文件。
- 示例中的 `requirements.txt` 为生成文件；应修改对应的 `.in` 文件并按文件头命令重新生成。
- 未经任务明确要求，不得新增重量级依赖。新增依赖必须说明用途、许可证、平台兼容性和不能使用现有依赖实现的原因。

## 7. 测试与验证

每个行为变更都必须有测试。测试应验证可观察行为和边界条件，不应只复刻实现细节。

动作与数据管线至少覆盖：

- 31D ↔ 32D 的补零和解包；
- 左右手交错与解交错的往返一致性；
- horizon、shape、dtype、单位和 padding 不变量；
- 非严格 `2:1` 时间轴下的 `aligned_index`；
- 可选字段缺失、字段存在但无效及兼容别名；
- 训练转换与推理逆转换的一致性。

OpenPI 的命令必须从 `openpi/` 目录执行。对 `openpi/` 下的 Python 改动，最低验证要求是：

```bash
cd openpi
uv run ruff check <changed-python-paths>
uv run ruff format --check <changed-python-paths>
uv run pytest <affected-test-paths> -m "not manual"
```

修改共享模型、训练配置、数据转换或公共接口时，应在环境允许的情况下扩大验证范围：

```bash
cd openpi
uv run pytest -m "not manual"
uv run pre-commit run --files <changed-files>
```

`manual` 测试可能下载 checkpoint 或依赖 GPU，默认测试命令不会自动排除它们，因此常规验证必须显式使用 `-m "not manual"`。只有在相关资源齐备且任务需要时才运行 manual、训练或真实机器人测试。

在根工具链建立前，只能对 `openpi/` 范围内的改动复用其 uv 环境。新增根包时必须先按第 3 节建立可复现的安装和测试入口，再记录并执行实际命令。不得用临时环境技巧冒充项目工具链，不得虚构 typecheck、coverage 或 CI 门禁，也不得声称未执行的测试已经通过。

从仓库根目录使用以下入口。基础动作与数据代码只需默认依赖；PyTorch 训练测试需要安装 `pytorch` extra。依赖发生变化时先执行 `uv lock` 并提交结果；日常验证必须使用锁定解析：

```bash
uv lock
uv sync --locked --extra pytorch
uv run --locked ruff check src tests
uv run --locked ruff format --check src tests
uv run --locked pytest tests -m "not manual"
```

需要与上游 OpenPI 联调时，应在 `openpi/` 的 uv 环境中以可编辑方式安装根包，不得使用临时 `PYTHONPATH`：

```bash
cd openpi
uv run --with-editable .. python -c "import pi_dex"
```

## 8. 标准工作流

1. 先阅读相关实现、相邻测试、配置和 `data/schema.json`；不要仅根据文件名猜测行为。
2. 在编码前写清输入、输出、shape、时间轴、坐标系和失败条件。
3. 选择影响范围最小的实现位置，优先在 `src/pi_dex/` 中通过适配复用 OpenPI。
4. 先补充或更新针对性测试，再完成最小实现。
5. 运行受影响测试、Ruff 检查和格式检查；高风险公共变更再运行更广测试。
6. 审阅最终 diff，移除调试代码，确认没有无关修改、敏感信息或大文件。
7. 同步更新相关文档、配置、schema 和示例。

## 9. 文档与兼容性

- 动作布局、数据字段、归一化、配置名称、CLI 或公共 API 发生变化时，必须在同一改动中更新对应文档和示例。
- 破坏兼容性的变更必须说明迁移方式；能够合理兼容旧格式时，应提供显式适配或版本判断。
- 不得在没有迁移说明的情况下更改 checkpoint shape、字段含义、左右手顺序或坐标系。
- 实验性接口必须明确标记其稳定性和预期替换点，避免下游将其误认为稳定 API。

## 10. 完成标准

任务完成时必须说明：

- 修改了什么，以及选择该实现位置的原因；
- 执行了哪些验证命令及其结果；
- 哪些验证未执行及原因；
- 已知风险、兼容性影响和后续事项。

不得提交原始数据、模型权重、checkpoint、缓存、实验日志、临时文件或任何凭据。仅当实现、测试、文档和交付说明彼此一致时，任务才视为完成。
